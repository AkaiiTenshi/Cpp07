# What is a template

A template in c++ is a patron, a model, of either a funtion or a class, with a specifique type of variable(e.g < typename T>) that we can use to lighten our code base. It basically prevents repetitive code. 
We can then instanciate that template, and at that time, the compiler will generate the corresponding code for us.

